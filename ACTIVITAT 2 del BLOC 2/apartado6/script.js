var primer = parseInt(prompt("introduce el primer numero"));

var segundo = parseInt(prompt("Introduce el segundo numero"));

var Suma = segundo + primer;

alert(Suma);
document.write('El resultado de la suma es ' + Suma)